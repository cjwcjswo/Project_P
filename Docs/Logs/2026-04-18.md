# 2026-04-18 작업 로그 — Day 4 Stage & Monster System

## 완료 태스크

### T-D4-001 MonsterData.json + MonsterDataRepository
- `Client/Assets/Resources/MonsterData.json` 생성 (4종: 슬라임1001, 강화슬라임1002, 화염오크2005, 어둠고블린2006)
- `Scripts/Data/MonsterDataRepository.cs` 신규 — Dictionary 캐시, GetById/GetAll

### T-D4-002 StageData.json + StageDataRepository
- `Client/Assets/Resources/StageData.json` 생성 (StageId 1 고블린숲 2웨이브, StageId 2 어둠의동굴 2웨이브)
- `Scripts/Data/StageDataRepository.cs` 신규 — Dictionary 캐시, GetById/GetAll/Exists

### GAP-2 BattleCompleteEvent struct 정의
- `Scripts/Battle/BattleEvents.cs` 신규 — `struct BattleCompleteEvent { BattleResult Result; }`

### GAP-3 EnemyState 시각 정보 추가
- `EnemyState.cs` 수정 — MonsterDataId, DisplayName, PrefabPath 프로퍼티 + 생성자 확장

### T-D4-003 + GAP-1 BattleSetupData + 멀티웨이브 BattleManager
- `Scripts/Battle/BattleSetupData.cs` 신규 — (StageData, MonsterDataRepository) → EnemyWave[] 빌드
- `Scripts/Battle/SkillSystem.cs` 수정 — SetWave(EnemyWave) 메서드 추가
- `BattleManager.cs` 전면 수정:
  - StartBattle 시그니처: (HeroParty, BattleSetupData, BoardController)
  - _currentWaveIndex 상태 + ActivateWave() + 멀티웨이브 진행 로직
  - HandleAllEnemiesDead: 다음 웨이브 진행 or 전투 승리 분기
  - BattleCompleteEvent(BattleResult) 발행
  - public event OnWaveChanged 추가

### T-D4-004 + GAP-4 에너지 시스템
- `UserDataDTO.cs` — UserGameDataDTO에 int energy 필드 추가
- `GameManager.cs` 전면 수정:
  - TryConsumeEnergy(int cost) 구현
  - MonsterDataRepository, StageDataRepository 초기화 및 ServiceLocator 등록
  - GameFlowManager 생성 및 등록
  - BattleSetupData 기반 전투 시작 (하드코딩 적 제거)
  - BattleSceneView.BindBattleManager() 호출 추가
- `Server/ProjectP.Server/Program.cs`:
  - UserGameDataResponse에 Energy 추가
  - POST /api/user/energy/consume 스텁
  - POST /api/battle/complete 스텁

### T-D4-005 전투 보상 시스템
- `BattleResult.cs` 신규 — StageId, DeployedHeroCount, SurvivedHeroIds, IsCleared
- `RewardCalculator.cs` 신규 — EXP FloorToInt 분배, Gold 계산

### T-D4-006 Stage Select UI
- `StageSelectView.cs` 신규 — ServiceLocator에서 StageDataRepository 취득 → StageCellView 동적 생성
- `StageCellView.cs` 신규 — 스테이지 정보 표시, TryConsumeEnergy 검사, LoadBattleScene 호출

### T-D4-007 Battle Reward UI
- `BattleRewardView.cs` 신규 — BattleCompleteEvent 구독, 히어로 EXP 목록, 3종 버튼
- `HeroExpRowView.cs` 신규 — 히어로 초상화(색상 표시) + EXP 텍스트 행

### T-D4-008 + GAP-5 GameFlowManager
- `GameFlowManager.cs` 신규 — 씬 전환 상태 머신, CurrentStageId, LoadBattleScene/LoadStageSelect/ShowRewardScreen
- `BattleSceneView.cs` 수정 — BindBattleManager(BattleManager) 추가, OnWaveChanged 핸들러로 적 뷰 갱신

---

# Day 5 - UserDataManager 분리 및 스테이지 클리어 기반 잠금

## 완료 태스크

### T-D5-001 UserDataDTO에 clearedStageIds 필드 추가
- `UserDataDTO.cs` — `UserGameDataDTO`에 `List<int> clearedStageIds` 필드 추가
- `Resources/default_user.json` — `energy`, `clearedStageIds: []` 필드 추가

### T-D5-002 UserDataManager 신규 생성
- `Scripts/Data/UserDataManager.cs` 신규 — 순수 C# 클래스
  - `InitializeAsync(ApiClient, userId)` — UserDataService 내부 호출, 데이터 초기화
  - `Energy`, `DeployedHeroes`, `ClearedStageIds` 읽기 전용 프로퍼티
  - `TryConsumeEnergy(cost)` — 에너지 차감 로직
  - `AddClearedStage(stageId)` — 클리어 이력 추가
  - `HasCleared(stageId)` — 클리어 여부 확인

### T-D5-003 GameManager에서 UserDataManager 초기화 및 위임
- `GameManager.cs` 수정:
  - `_gameData` 필드 제거, `UserDataManager` 인스턴스로 교체
  - `InitializeServicesAsync()`에서 `UserDataManager.InitializeAsync()` await
  - `ServiceLocator.Register(userDataManager)` 등록
  - `TryConsumeEnergy()` → `UserDataManager.TryConsumeEnergy()` 위임
  - `DeployedHeroes` 접근 → `userDataManager.DeployedHeroes`

### T-D5-004 BattleRewardView에 클리어 이력 저장 연결
- `BattleRewardView.cs` 수정:
  - `OnBattleComplete()` 내 `result.IsCleared == true`이면 `UserDataManager.AddClearedStage()` 호출

### T-D5-005 StageSelectView 클리어 기반 상태 분류
- `StageSelectView.cs` 전면 수정:
  - `UserDataManager.ClearedStageIds` 조회
  - `isCleared`, `isAvailable` 계산 (i==0 || 직전 스테이지 클리어)
  - `StageCellView.Bind(stageData, isCleared, isAvailable)` 호출

### T-D5-006 StageCellView 3-상태 Bind 시그니처 변경
- `StageCellView.cs` 수정:
  - `Bind(StageData, bool isCleared, bool isAvailable)` 시그니처 변경
  - `_lockOverlay`, `_clearBadge` SerializeField 추가
  - `isAvailable`에 따라 버튼 interactable 설정

### T-D5-007 서버 clearedStageIds 응답 추가
- `Server/Program.cs` 수정:
  - `userClearedStagesDb` 인메모리 딕셔너리 추가
  - `UserGameDataResponse`에 `ClearedStageIds` 파라미터 추가
  - `POST /api/stage/clear` 스텁 엔드포인트 추가
  - `StageClearRequest` 레코드 추가

## Unity 에디터 세팅 필요 사항

아래 작업은 유니티 에디터에서 직접 수행해야 함:

**Day 4 세팅:**
1. **StageSelectScene 신규 생성**: File > New Scene, "StageSelect"로 저장
2. **StageCellView 프리팹**: `Assets/Resources/Prefabs/StageCellView.prefab` 생성
3. **HeroExpRow 프리팹**: `Assets/Resources/Prefabs/HeroExpRow.prefab` 생성
4. **Build Settings**: StageSelectScene, BattleScene 등록
5. **Inspector 연결**: 각 View 컴포넌트 필드 연결

**Day 5 세팅 (StageCellView 프리팹 수정):**
1. Project 창에서 `StageCellView` 프리팹 열기
2. 루트 하위에 **LockOverlay** Image 오브젝트 추가 (반투명 검정 배경 + 자물쇠 텍스트/아이콘)
3. 루트 하위에 **ClearBadge** 오브젝트 추가 (예: "CLEAR" TextMeshPro 텍스트, 초록색)
4. `StageCellView` 컴포넌트 Inspector의 `Lock Overlay` 슬롯 → `LockOverlay` 오브젝트 연결
5. `Clear Badge` 슬롯 → `ClearBadge` 오브젝트 연결
6. `LockOverlay` 기본값 **활성화**, `ClearBadge` 기본값 **비활성화**로 설정 후 저장

---

# Day 5+ - GameManager / BattleSceneManager 책임 분리

## 완료 태스크

### GameManager 슬림화
- `GameManager.cs` 수정:
  - `PuzzleBoardView`, `BattleHUD`, `BattleSceneView` SerializeField 제거
  - `_battleManager` 필드 제거
  - HeroParty 구성, Board/BattleManager/UI 바인딩 로직 전부 제거
  - 메서드명 `InitializeServicesAsync` → `InitializeCoreServicesAsync`로 변경
  - 남은 책임: UserDataManager 로드, MonsterRepo/StageRepo/GameFlowManager 등록, TryConsumeEnergy 위임

### BattleSceneManager 신규 생성
- `Scripts/Core/BattleSceneManager.cs` 신규:
  - Battle 씬 전용 MonoBehaviour (DontDestroyOnLoad 없음)
  - `WaitForService<T>()` 헬퍼로 GameManager 비동기 초기화 완료 대기
  - `GameFlowManager.CurrentStageId`로 스테이지 결정 (0이면 fallback stageId=1)
  - HeroParty 구성, Board/BoardController 초기화, BattleManager.StartBattle()
  - PuzzleBoardView / BattleHUD / BattleSceneView 바인딩

## Unity 에디터 세팅 필요 사항

**GameManager 오브젝트 (초기 씬):**
- `Puzzle Board View`, `Battle HUD`, `Battle Scene View` 슬롯 없어야 정상
- `Server Base Url`, `User Id` 값만 유지

**BattleSceneManager 오브젝트 (Battle 씬):**
1. `Battle.unity` 씬 열기
2. 빈 GameObject 생성 → 이름 `BattleSceneManager`
3. `BattleSceneManager` 컴포넌트 추가
4. Inspector 슬롯 연결:
   - `Puzzle Board View` → 씬의 `PuzzleBoardView` 오브젝트
   - `Battle HUD` → 씬의 `BattleHUD` 오브젝트
   - `Battle Scene View` → 씬의 `BattleSceneView` 오브젝트
5. Battle 씬에 `GameManager` 오브젝트가 별도로 있었다면 제거

---

# 웨이브 전환 DOTween 정리

- `EnemyEntityView.cs`: `_boundEnemy` 보관, 이벤트는 이름 있는 핸들러로 구독/해제, `UnbindAndKillTweens()`에서 구독 해제 + `DOTween.Kill`/`DOKill`, `PlayDeathAnim` `OnComplete`에 `this == null` 가드, 트윈에 `SetLink(gameObject)`, `OnDestroy`에서도 `UnbindAndKillTweens()` 호출.
- `BattleSceneView.cs`: `OnWaveChanged`에서 적 뷰 `Destroy` 직전 `UnbindAndKillTweens()` 호출.
