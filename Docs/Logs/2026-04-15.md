# 2026-04-15 작업 로그

## Day 2 — QA & Visual Improvements

### T-D2-001: 블록 스왑 실패 시 원위치 복귀 버그 수정 ✅

**원인:** `PuzzleBoardView.OnSwap`에서 `IsValid = false` 이벤트를 `return`으로 무시하여 뷰 복귀 처리 미실행.

**수정 파일:**
- `Client/Assets/_Project/Scripts/UI/PuzzleBoardView.cs`
  - `OnSwap`에서 `if (!evt.IsValid) return;` 제거
  - `IsValid` 여부에 따라 ease 분기: 정방향 `OutBounce`, 복귀 `InOutSine`
- `Client/Assets/_Project/Scripts/UI/BlockView.cs`
  - `AnimateMoveTo(Vector3, float, Ease)` — Ease 파라미터 추가 (기본값 `OutBounce` 유지)

---

### T-D2-002: Player/Enemy 데미지 플로팅 텍스트 표시 ✅

**수정 파일:**
- `Client/Assets/_Project/Scripts/Battle/HeroState.cs`
  - `public event Action<int> OnDamageTaken;` 추가
  - `TakeDamage` 내 `finalDamage` 계산 후 이벤트 발행
- `Client/Assets/_Project/Scripts/Battle/EnemyState.cs`
  - `public event Action<int> OnDamageTaken;` 추가
  - `TakeDamage` 내 `actual` 계산 후 이벤트 발행
- `Client/Assets/_Project/Scripts/Core/GameManager.cs`
  - `BattleSceneView.Bind` 호출에 `enemy` 파라미터 추가

**신규 파일:**
- `Client/Assets/_Project/Scripts/UI/FloatingTextView.cs`
  - `TextMeshPro` 기반 월드 스페이스 플로팅 텍스트
  - DOTween: 1초 위로 이동(OutCubic) + 마지막 0.4초 페이드 아웃(InQuad) → Destroy

---

### T-D2-003: 스킬 발동 시 Player 위에 스킬명 텍스트 표시 ✅

---

## Day 2 추가 개선사항

### T-D2-004: Monster 평타 공격 추가 ✅

**수정 파일:**
- `Client/Assets/_Project/Scripts/Battle/EnemyState.cs`
  - `AutoAttackInterval(3.0f)`, `AutoAttackRatio(0.1f)` 프로퍼티 추가
  - `OnAutoAttack` 이벤트, `GetAutoAttackDamage()`, `NotifyAutoAttack()` 추가
  - 생성자에 `autoAttackInterval`, `autoAttackRatio` 파라미터 추가
- `Client/Assets/_Project/Scripts/Battle/BattleManager.cs`
  - `EnemyAutoAttackLoop` 추가 (3초 간격, 기절 시 스킵), `StartBattle`에서 루프 시작
- `Client/Assets/_Project/Scripts/UI/BattleSceneView.cs`
  - `OnEnemyAutoAttack` 핸들러 추가: 적 펀치 + 영웅 피격 플래시
  - `OnDestroy`에서 구독 해제 추가

---

### T-D2-005: FloatingTextView / BlockView 오브젝트 풀링 ✅

**수정 파일:**
- `Client/Assets/_Project/Scripts/UI/FloatingTextView.cs`
  - `Destroy(gameObject)` → `SetActive(false) + OnRelease?.Invoke(this)` 로 교체
  - `Action<FloatingTextView> OnRelease` 프로퍼티 추가 (풀 소유자가 설정)
- `Client/Assets/_Project/Scripts/UI/BattleSceneView.cs`
  - `Queue<FloatingTextView> _floatingPool` 추가
  - `GetFromFloatingPool()`, `ReturnToFloatingPool()` 추가
  - `SpawnDamageText`, `SpawnSkillText`에서 `Instantiate` → 풀 사용으로 교체
- `Client/Assets/_Project/Scripts/UI/BlockView.cs`
  - `AnimateDestroy(float duration, Action onComplete = null)` — `onComplete` 파라미터 추가
  - `System` using 추가
- `Client/Assets/_Project/Scripts/UI/PuzzleBoardView.cs`
  - `Queue<BlockView> _blockPool` 추가
  - `GetBlockFromPool(Vector3)` 추가
  - `SpawnAllBlocks`, `OnGravityRefill(RefillMoves)`: `Instantiate` → `GetBlockFromPool` 교체
  - `OnMatchFound`: `AnimateDestroy`에 `() => _blockPool.Enqueue(view)` 콜백 전달

---

### T-D2-006: 연속 피격 시 색상 영구 변경 버그 수정 ✅

**원인:** `DOColor(flash).OnComplete(() => DOColor(white))` 패턴에서 연속 피격 시 진행 중인 tween이 kill되면 OnComplete가 호출되지 않아 원본 색상으로 복귀되지 않음.

**수정 파일:**
- `Client/Assets/_Project/Scripts/UI/BattleSceneView.cs`
  - `_heroBaseColor`, `_enemyBaseColor` 필드 추가 (Bind에서 원본 색상 저장)
  - `FlashColor(SpriteRenderer, Color flashColor, Color baseColor, float duration)` 헬퍼 추가
    - `sprite.DOKill(false)` 후 단일 Sequence(flash → baseColor 복귀) 실행
  - 모든 `DOColor(...).OnComplete(...)` 체인 → `FlashColor(...)` 호출로 교체

**수정 파일:**
- `Client/Assets/_Project/Scripts/UI/BattleSceneView.cs`
  - `Bind` 시그니처에 `EnemyState enemy` 파라미터 추가
  - `_hero.OnDamageTaken`, `_enemy.OnDamageTaken` 구독 → `SpawnDamageText` 호출
  - `OnSkillExecuted` 핸들러에 `SpawnSkillText` 추가 (BlockType → 스킬명 + 색상 매핑)
  - `SpawnDamageText(int, Vector3)`, `SpawnSkillText(BlockType, Vector3)` 메서드 신규 추가
