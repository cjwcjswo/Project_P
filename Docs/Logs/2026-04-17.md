## 2026-04-17

- `PuzzleBoardView` 레이아웃 계산 개선: 화면 하단 50% 영역(세로 제약 유지) 내에서 padding(상/좌/우/하) 적용 및 `_cellStep`(센터 간격) / `_blockWorldSize`(블록 실제 크기) 분리.
- `BlockView`에 `SetWorldSize(float)` 추가: 목표 월드 사이즈에 맞춰 스프라이트 스케일 조정, 블록 간 gap 조절이 실제 비주얼에 반영되도록 개선.
- 버그 수정: 리필 스폰 애니메이션(`BlockView.AnimateSpawn`)이 스케일을 `Vector3.one`으로 덮어써 새 블록이 예전 크기로 보이던 문제 해결.
- `PuzzleBoardView`: `BlockType`별 `BlockView` 프리팹 인스펙터 매핑, 프리팹 키 기반 풀, `EnsureBlockViewAt`로 스폰/교체. `BlockView`: `SourcePrefab`/`BindSourcePrefab`으로 풀 반환 키 보관.
# 작업 로그 2026-04-17

## Battle World Sprite Multi-Entity 구현 완료

### 목적
`BattleSceneView`의 단일 `_heroSprite`/`_enemySprite` 참조를 제거하고,
히어로/적 수에 따라 `SpawnPoint` 리스트에 프리팹을 동적 스폰하여 다중 엔티티 월드 스프라이트 지원.

---

### 구현 파일

#### 신규 생성
- **`Client/Assets/_Project/Scripts/UI/HeroEntityView.cs`**
  - 히어로 1명의 월드 스프라이트 컴포넌트
  - `Bind(HeroState)` — `partyIndex` 저장, `OnDamageTaken`/`OnDeath` 이벤트 구독
  - `PlayAttackAnim()` — DOPunchPosition (우측)
  - `PlayDamageFlash(Color)` — 붉은 플래시
  - `PlayColorFlash(Color)` — 힐/방어막 시 색상 강조
  - `PlayDeathAnim()` — 회색화 + 반투명 처리

- **`Client/Assets/_Project/Scripts/UI/EnemyEntityView.cs`**
  - 적 1마리의 월드 스프라이트 컴포넌트
  - `Bind(EnemyState)` — `waveIndex` 저장, `OnDamageTaken`/`OnDeath` 이벤트 구독
  - `PlayAttackAnim()` — DOPunchPosition (좌측)
  - `PlayHitFlash(Color)` — 단일 피격 플래시
  - `PlayAoEHitAnim(Color)` — 광역 피격 + DOShakePosition
  - `PlayDeathAnim()` — 페이드 아웃

#### 수정
- **`Client/Assets/_Project/Scripts/UI/BattleSceneView.cs`**
  - 필드 제거: `_heroSprite`, `_enemySprite`, `_heroBaseColor`, `_enemyBaseColor`
  - 필드 추가: `_heroEntityPrefab`, `_heroSpawnPoints[]`, `_enemyEntityPrefab`, `_enemySpawnPoints[]`
  - 딕셔너리 추가: `Dictionary<int, HeroEntityView> _heroViews`, `Dictionary<int, EnemyEntityView> _enemyViews`
  - `Bind()` 시그니처에 `TargetingSystem targeting` 파라미터 추가
  - `SpawnHeroes()`, `SpawnEnemies()` — 파티/웨이브 인원만큼 프리팹 스폰 후 딕셔너리 등록
  - `OnHeroAutoAttack(partyIndex)`, `OnEnemyAutoAttack(waveIndex)` — 딕셔너리 조회로 개별 애니메이션
  - `OnSkillExecuted` — `HeroColorMap.GetHeroIndex()`로 소스 히어로 특정, `TargetingSystem`으로 타겟 적 특정

- **`Client/Assets/_Project/Scripts/Core/GameManager.cs`**
  - `_battleSceneView.Bind()` 호출에 `_battleManager.Targeting` 전달

---

---

## Day 3 QA & Improvements

### T-D3-001: BattleHUD.cs
- `BindParty()` / `BindWave()` 내 `Instantiate` 직후 `view.gameObject.SetActive(true)` 추가
- HUD 슬롯 프리팹이 비활성 상태로 생성되던 버그 수정

### T-D3-002: BattleSceneView.cs
- `SpawnHeroes()` 내 `heroes[i].OnDamageTaken` 구독 추가 → `SpawnDamageText(dmg, view.WorldPosition + Vector3.up * 0.5f)`
- `SpawnEnemies()` 내 `enemies[i].OnDamageTaken` 구독 추가 → `SpawnDamageText(dmg, view.WorldPosition + Vector3.up * 0.5f)`
- 다중 EntityView 리팩터링 시 누락됐던 데미지 플로팅 텍스트 복구

### T-D3-003: 스킬 즉시 발동 개선
- `Events.cs`: `MatchStepSkillTriggerEvent` 추가 (ColorBreakdown, ComboStep)
- `BoardController.cs`:
  - `BuildStepColorData()` 헬퍼 추가 (스텝별 색상 데이터 수집)
  - `TrySwapAsync`, `ProcessCascadeAsync` 루프 양쪽에 `MatchStepSkillTriggerEvent` 발행 추가
- `BattleManager.cs`:
  - `OnMatchStepSkillTrigger()` 신규 핸들러: 콤보 증가 + 스킬 실행 + 궁극기 충전
  - `OnCascadeComplete()`: 콤보 리셋만 수행으로 역할 축소
  - `Cleanup()`: `MatchStepSkillTriggerEvent` 구독 해제 추가

---

### Unity 에디터 세팅 필요 사항

#### HeroEntityView 프리팹 신규 생성
1. 빈 오브젝트에 `SpriteRenderer` + `HeroEntityView` 스크립트 부착
2. `_spriteRenderer` 필드에 해당 SpriteRenderer 할당
3. 임시 스프라이트(플레이스홀더) 할당
4. `Assets/_Project/Prefabs/` 경로에 저장

#### EnemyEntityView 프리팹 신규 생성
1. 빈 오브젝트에 `SpriteRenderer` + `EnemyEntityView` 스크립트 부착
2. `_spriteRenderer` 필드에 해당 SpriteRenderer 할당
3. `Assets/_Project/Prefabs/` 경로에 저장

#### BattleSceneView 씬 오브젝트 업데이트
1. 인스펙터에서 기존 `_heroSprite`, `_enemySprite` 필드 제거 확인
2. `_heroEntityPrefab` → 위에서 만든 HeroEntityView 프리팹 할당
3. `_heroSpawnPoints` → 씬에 빈 GameObjects 5개 배치 후 배열 할당
   - `[0]`: 아군 진영 최전방 (좌측 끝)
   - `[1]~[4]`: 뒤로 순서대로
4. `_enemyEntityPrefab` → EnemyEntityView 프리팹 할당
5. `_enemySpawnPoints` → 씬에 빈 GameObjects 배치 후 배열 할당
   - `[0]`: 적군 진영 최전방 (우측 끝)
